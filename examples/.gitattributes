# Sempress Git LFS Configuration Examples

# Basic: Track all CSV files with Sempress compression
*.csv filter=lfs-sempress diff=lfs merge=lfs -text

# Track CSV files in specific directories
data/*.csv filter=lfs-sempress diff=lfs merge=lfs -text
datasets/**/*.csv filter=lfs-sempress diff=lfs merge=lfs -text

# Track Parquet files (if Sempress adds Parquet support)
# *.parquet filter=lfs-sempress diff=lfs merge=lfs -text

# Track large JSON files with numeric data
# large-data/*.json filter=lfs-sempress diff=lfs merge=lfs -text

# Exclude certain patterns from compression
# These files will use regular Git (no LFS, no Sempress)
tests/*.csv !filter
sample/*.csv !filter

# You can mix Sempress with regular Git LFS for other files
# Images with regular LFS (no Sempress compression)
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text

# Binary files with regular LFS
*.bin filter=lfs diff=lfs merge=lfs -text
*.dat filter=lfs diff=lfs merge=lfs -text
